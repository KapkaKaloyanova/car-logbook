import { ExpiryWarningDirective } from './expiry-warning.directive';

describe('ExpiryWarningDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpiryWarningDirective();
    expect(directive).toBeTruthy();
  });
});
