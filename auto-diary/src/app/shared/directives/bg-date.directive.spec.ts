import { BgDateDirective } from './bg-date.directive';

describe('BgDateDirective', () => {
  it('should create an instance', () => {
    const directive = new BgDateDirective();
    expect(directive).toBeTruthy();
  });
});
